#!/bin/bash
JWODDER_ROOT="$(dirname "$(dirname "$(readlink -f "$0")")")"
PYTHONPATH="$JWODDER_ROOT/lib" \
    "$JWODDER_ROOT/venvs/logsdb/python" -m jwodder_logsdb.apache_access \
    2>>/var/log/jwodder/apache_access.err
