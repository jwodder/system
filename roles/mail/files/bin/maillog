#!/bin/bash
JWODDER_ROOT="$(dirname "$(dirname "$(readlink -f "$0")")")"
"$JWODDER_ROOT"/venvs/logsdb/bin/logsdb \
    -c "$JWODDER_ROOT"/etc/logsdb.ini \
    maillog \
    2>>"$JWODDER_ROOT"/logs/maillog.err
